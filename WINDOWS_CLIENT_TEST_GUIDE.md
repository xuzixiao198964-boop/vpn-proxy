# 🖥️ Windows VPN代理客户端测试指南

## 📋 测试目标
验证Windows版VPN代理客户端可以连接到服务器SSH服务（104.244.90.202:22）

## ✅ 准备工作已完成

### 1. 服务器验证 ✅
- **地址**: `104.244.90.202`
- **TLS隧道端口**: `18443` ✅ 开放
- **SSH端口**: `22` ✅ 开放
- **用户名**: `root`
- **密码**: `v9wSxMxg92dp`

### 2. 客户端配置 ✅
- **配置文件**: `C:\Users\Administrator\.vpnproxy_client.json`
- **已预配置服务器信息**
- **CA证书**: `server\data\certs\server.crt` ✅ 存在

### 3. 依赖安装 ✅
- **Python 3.13.1** ✅
- **cryptography库** ✅ 已安装

## 🚀 测试步骤

### 步骤1：启动Windows客户端GUI
```bash
# 在项目根目录运行
python -m windows_client.app_gui
```

或者：
```bash
cd windows_client
python app_gui.py
```

### 步骤2：GUI界面配置
1. **服务端地址**: `104.244.90.202`（已预填）
2. **端口**: `18443`（已预填）
3. **用户名**: `root`（已预填）
4. **密码**: `v9wSxMxg92dp`（需要手动输入）
5. **CA证书路径**: `server\data\certs\server.crt`（已预填）
6. **本地SOCKS5**: `127.0.0.1:1080`（已预填）

### 步骤3：测试连接
1. 点击 **"启动代理"** 按钮
2. 观察日志区域显示连接状态
3. 等待显示 **"隧道已建立，本地 SOCKS5 已启动"**

### 步骤4：验证SSH连接
连接建立后，可以通过SOCKS5代理测试SSH连接：
```bash
# 使用curl测试（通过SOCKS5代理）
curl --socks5 127.0.0.1:1080 http://ifconfig.me

# 或者使用其他支持SOCKS5的工具测试SSH连接
```

## 🔧 故障排除

### 问题1：GUI启动失败
```bash
# 检查Python模块路径
python -c "import sys; print(sys.path)"

# 确保在项目根目录运行
cd E:\work\vpn-proxy-client
python -m windows_client.app_gui
```

### 问题2：连接失败
1. **检查服务器端口**：
   ```bash
   telnet 104.244.90.202 18443
   ```
   
2. **检查CA证书**：
   ```bash
   # 确保证书文件存在
   dir server\data\certs\server.crt
   ```

3. **检查防火墙**：
   - 确保Windows防火墙允许Python连接
   - 确保没有其他安全软件阻止连接

### 问题3：密码错误
- 用户名：`root`
- 密码：`v9wSxMxg92dp`
- 注意大小写

## 📊 预期结果

### 成功连接的表现
1. GUI日志显示：**"正在连接…"**
2. 然后显示：**"隧道已建立，本地 SOCKS5 已启动。"**
3. 按钮状态变化：启动按钮禁用，停止按钮启用
4. 可以在日志区域看到连接状态信息

### 连接后的验证
1. **本地SOCKS5代理**：`127.0.0.1:1080` 可用
2. **网络流量**：通过隧道转发
3. **SSH连接**：可以通过代理连接到 `104.244.90.202:22`

## ⚙️ 技术细节

### 客户端架构
- **前端**: Tkinter GUI
- **隧道协议**: TLS加密隧道
- **代理类型**: SOCKS5
- **多路复用**: 支持多个连接通过单个隧道

### 配置文件位置
```
C:\Users\Administrator\.vpnproxy_client.json
```

### 日志位置
- GUI界面内嵌日志显示
- 控制台输出（如果从命令行启动）

## 🎯 测试重点

### 核心功能测试
- [ ] GUI启动正常
- [ ] 配置加载正确
- [ ] TLS隧道建立成功
- [ ] SOCKS5代理启动
- [ ] 可以通过代理连接SSH

### 错误处理测试
- [ ] 错误密码提示
- [ ] 网络断开重连
- [ ] 证书错误处理
- [ ] 优雅停止

## ⏱️ 测试时间预估
- **GUI启动**: 1-2分钟
- **连接测试**: 2-3分钟
- **功能验证**: 3-5分钟
- **总计**: 6-10分钟

## 📞 支持信息
- **服务器**: 104.244.90.202:18443
- **SSH服务**: 104.244.90.202:22
- **测试时间**: 2026-03-25 22:35
- **客户端版本**: Python GUI 1.0

---

**下一步**: 启动GUI，输入密码，点击"启动代理"，观察连接状态。